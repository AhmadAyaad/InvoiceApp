import { environment } from 'src/environments/environment';

export const baseUrl = 'http://localhost:59443';

export const unitsUrl = baseUrl + '/api/unit';
export const storesUrl = baseUrl + '/api/store';
export const productsUrl = baseUrl + '/api/products';